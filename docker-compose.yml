services:
  postgres:
    image: postgres:16-alpine
    container_name: mcp-secure-db-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-mcp_experiment}
      POSTGRES_USER: ${POSTGRES_USER:-mcp_user}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-change-me}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./data/sakila-postgres.sql:/docker-entrypoint-initdb.d/01_sakila.sql:ro
      - ./data/seed_indirect_injection.sql:/docker-entrypoint-initdb.d/02_seed_indirect_injection.sql:ro
      - ./database/views.sql:/docker-entrypoint-initdb.d/03_views.sql:ro
      - ./database/permissions.sql:/docker-entrypoint-initdb.d/04_permissions.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-mcp_user} -d ${POSTGRES_DB:-mcp_experiment}"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
